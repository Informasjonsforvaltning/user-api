spring:
  jackson:
    default-property-inclusion: non_null
    date-format: "yyyy-MM-dd'T'HH:mm:ss.SSS'Z'"
  data.mongodb.uri: mongodb://${MONGO_USERNAME}:${MONGO_PASSWORD}@mongodb:27017/user-api?authSource=admin&authMechanism=SCRAM-SHA-1
logging:
  level.root: WARN
  level.no: ${LOG_LEVEL:DEBUG}
  level.org.springframework: WARN
  level.org.springframework.web: WARN
application:
  hosts:
    altinnProxyHost: ${ALTINN_PROXY_HOST:http://fdk-nginx-altinn-proxy:8080}
  whitelists:
    # 920210023   /* this matches in prod system "GJELDSREGISTERET AS”*/
    # 910258028   /* this matches in mock data "LILAND OG ERDAL REVISJON", used in dev, ut1, st1 and it1*/
    orgNrWhitelist: ${ORGNR_WHITELIST:920210023,910258028}
    orgFormWhitelist: ${ORGFORM_WHITELIST:ADOS,FKF,FYLK,IKS,KF,KIRK,KOMM,ORGL,SF,STAT,SÆR}
    adminList: ${ADMIN_LIST:23076102252} #MAUD GULLIKSEN

security.oauth2.resource:
  id: user-api
  jwk.key-set-uri: ${JWK_KEY_SET_URI:http://sso:8084/auth/realms/fdk/protocol/openid-connect/certs}

---
#Utv-miljø lokalt på egen PC
spring:
  profiles: develop
  data.mongodb.uri: mongodb://admin:admin@localhost:27017/user-api?authSource=admin&authMechanism=SCRAM-SHA-1
application.hosts.altinnProxyHost: http://localhost:8128
server.port: 8080
security.oauth2.resource.jwk.key-set-uri: http://localhost:8084/jwk